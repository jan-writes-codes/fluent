Django>=4.2,<5.0

# --- Production serving ---
gunicorn>=21,<23          # WSGI app server (replaces `runserver` in prod)
whitenoise>=6,<7          # serves static files (logo, fonts, JS) when DEBUG is off

# --- Payments ---
stripe>=8,<13            # Stripe Checkout for self-service credit top-ups (optional at runtime)

# --- Transactional email ---
django-anymail[resend]>=10,<13   # ESP-agnostic email backend (Resend) + delivery webhooks

# --- Database configuration ---
dj-database-url>=2,<3     # parses the DATABASE_URL env var into Django's DATABASES
psycopg[binary]>=3.1,<4   # PostgreSQL driver (only used when DATABASE_URL points at Postgres)
