# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/

# OS
.DS_Store

# Node (frontend test deps)
node_modules/

# Local SQLite database — may hold real personal data in production; never commit
db.sqlite3

# Uploaded media
media/

# Collected static files (regenerated by `collectstatic` on deploy)
staticfiles/

# Environment files (hold secrets — never commit)
.env
.env.*

# Local throwaway venv (Python 3.11) used for running tests
.venv2/
