{
  "name": "fluent-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Headless jsdom probe used by the Django test suite to assert client-side behaviour of the Fluent SPA.",
  "dependencies": {
    "jsdom": "^24.0.0"
  }
}
